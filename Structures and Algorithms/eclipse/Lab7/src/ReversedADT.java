public abstract interface ReversedADT extends SortedADT{
	// returns result of reversing the ReversedADT object on which
	// the method is called without affecting the object
	public abstract ReversedADT reverse();
}
